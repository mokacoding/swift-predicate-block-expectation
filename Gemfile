# Read the Ruby version to use with Bundler from the .ruby-version file in the
# repo. This will help users of the repo (assuming the use a Ruby version
# manager) and deployment tools use the same Ruby version.
ruby File.read("#{File.dirname(__FILE__)}/.ruby-version")

source 'https://rubygems.org' do
  gem 'xcpretty'
end
